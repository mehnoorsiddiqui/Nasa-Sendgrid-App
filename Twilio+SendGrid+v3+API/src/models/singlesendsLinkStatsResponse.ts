/**
 * Twilio SendGrid v3 APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, lazy, number, object, optional, Schema } from '../schema';
import { Metadata1, metadata1Schema } from './metadata1';
import { Results6, results6Schema } from './results6';

export interface SinglesendsLinkStatsResponse {
  /** This is the index of the link's location in the email contents. */
  results: Results6[];
  metadata: Metadata1;
  totalClicks?: number;
}

export const singlesendsLinkStatsResponseSchema: Schema<SinglesendsLinkStatsResponse> = object(
  {
    results: ['results', array(lazy(() => results6Schema))],
    metadata: ['_metadata', lazy(() => metadata1Schema)],
    totalClicks: ['total_clicks', optional(number())],
  }
);
