/**
 * Twilio SendGrid v3 APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, lazy, object, optional, Schema, string } from '../schema';
import { EmailConfig, emailConfigSchema } from './emailConfig';
import { SendTo, sendToSchema } from './sendTo';
import { Status1Enum, status1EnumSchema } from './status1Enum';
import { Warning, warningSchema } from './warning';

export interface SinglesendResponse {
  /** The name of the Single Send. */
  name: string;
  /** The categories to associate with this Single Send. */
  categories?: string[];
  /** The ISO 8601 time at which to send the Single Send. This must be in future or the string "now". Emails can be scheduled up to 72 hours in advance. However, this scheduling constraint does not apply to campaigns sent via [Marketing Campaigns](https://docs.sendgrid.com/ui/sending-email/how-to-send-email-with-marketing-campaigns/). */
  sendAt?: string;
  sendTo?: SendTo;
  emailConfig?: EmailConfig;
  id: string;
  status: Status1Enum;
  /** the ISO 8601 time at which the Single Send was last updated */
  updatedAt?: string;
  /** the ISO 8601 time at which the Single Send was created */
  createdAt: string;
  warnings?: Warning[];
}

export const singlesendResponseSchema: Schema<SinglesendResponse> = object({
  name: ['name', string()],
  categories: ['categories', optional(array(string()))],
  sendAt: ['send_at', optional(string())],
  sendTo: ['send_to', optional(lazy(() => sendToSchema))],
  emailConfig: ['email_config', optional(lazy(() => emailConfigSchema))],
  id: ['id', string()],
  status: ['status', status1EnumSchema],
  updatedAt: ['updated_at', optional(string())],
  createdAt: ['created_at', string()],
  warnings: ['warnings', optional(array(lazy(() => warningSchema)))],
});
