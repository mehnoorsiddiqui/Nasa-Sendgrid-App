/**
 * Twilio SendGrid v3 APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, boolean, lazy, number, object, Schema } from '../schema';
import { Ip1, ip1Schema } from './ip1';

export interface V3IpsResponse {
  /** List of IP objects. */
  ips: Ip1[];
  /** The number of IPs that can still be added to the user. */
  remainingIps: number;
  /** Whether or not the IPs are being warmed up. */
  warmup: boolean;
}

export const v3IpsResponseSchema: Schema<V3IpsResponse> = object({
  ips: ['ips', array(lazy(() => ip1Schema))],
  remainingIps: ['remaining_ips', number()],
  warmup: ['warmup', boolean()],
});
