/**
 * Twilio SendGrid v3 APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  nullable,
  number,
  object,
  optional,
  Schema,
  string,
} from '../schema';

export interface V3IpsResponse1 {
  /** An IP address. */
  ip: string;
  /** The subusers that are able to send email from this IP. */
  subusers: string[];
  /** The reverse DNS record for this IP address. */
  rdns?: string;
  /** The IP pools that this IP has been added to. */
  pools: string[];
  /** Indicates if this IP address is currently warming up. */
  warmup: boolean;
  /** The date that the IP address was entered into warmup. */
  startDate: number | null;
  /** Indicates if this IP address is associated with a reverse DNS record. */
  whitelabeled: boolean;
  /** The date that the IP address was assigned to the user. */
  assignedAt: number | null;
}

export const v3IpsResponse1Schema: Schema<V3IpsResponse1> = object({
  ip: ['ip', string()],
  subusers: ['subusers', array(string())],
  rdns: ['rdns', optional(string())],
  pools: ['pools', array(string())],
  warmup: ['warmup', boolean()],
  startDate: ['start_date', nullable(number())],
  whitelabeled: ['whitelabeled', boolean()],
  assignedAt: ['assigned_at', nullable(number())],
});
