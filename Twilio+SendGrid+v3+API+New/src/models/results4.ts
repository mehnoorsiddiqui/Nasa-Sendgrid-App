/**
 * Twilio SendGrid v3 APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, optional, Schema, string } from '../schema';
import { AbPhaseEnum, abPhaseEnumSchema } from './abPhaseEnum';
import { Metrics2, metrics2Schema } from './metrics2';

export interface Results4 {
  /** This is the ID of the Single Dend you require stats for. */
  id: string;
  /** This is the A/B variation of the Single Send stat returned. If the `group_by` parameter doesn't include `ab_variation` in the request, then the value is "all". */
  abVariation: string;
  abPhase: AbPhaseEnum;
  /** This describes the time unit to which the stat is rolled up. It is based on the `aggregated_by` parameter included in the request. It can be "total" or the date (in YYYY-MM-DD format) the stats are for. */
  aggregation?: string;
  stats?: Metrics2;
}

export const results4Schema: Schema<Results4> = object({
  id: ['id', string()],
  abVariation: ['ab_variation', string()],
  abPhase: ['ab_phase', abPhaseEnumSchema],
  aggregation: ['aggregation', optional(string())],
  stats: ['stats', optional(lazy(() => metrics2Schema))],
});
